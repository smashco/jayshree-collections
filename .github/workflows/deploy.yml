# GitHub Pages deployment disabled — site is deployed via Railway
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages disabled. Deploying via Railway."
